# Copy to .env.local to override. Both default to the values used by
# `make dev` / `uvicorn main:app` / core/crawler.py's UIWebSocketGateway.
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8765
